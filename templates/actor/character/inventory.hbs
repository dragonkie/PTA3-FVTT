<div class="pta-tab-inventory">
    <span class="items-header flexrow">
        <span class="item-name">Inventory</span>
        <span>Controls</span>
    </span>
    <ol class="pta-inventory items-list">
        {{#each items as | item key |}}
            {{> "pta.list-item" item=item}}
        {{/each}}
    </ol>
</div>