document.addEventListener('DOMContentLoaded', () => {
    for (const ele of document.querySelectorAll('.pokemon[data-status]')) {
        let conditions = ele.dataset.status.split(' ');
         
    }
});